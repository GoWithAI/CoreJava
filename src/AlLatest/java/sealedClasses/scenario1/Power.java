package AlLatest.java.sealedClasses.scenario1;

class Power { //AIM : we do not want you to extend this class, removed public so it can extend but woff
    // the can not use it now outside package as it is not public
//
}
