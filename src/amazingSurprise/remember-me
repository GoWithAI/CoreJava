1. more restrictive access can’t be given to the overridden function
2. T1.start(); if called twice -> runtime error
3. hashCode & equals ?
4.  static variables are called when a class loads and static variables are called only once.